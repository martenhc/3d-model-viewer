import {css} from 'lit';

export const buttonStyle = css`
  button {
    background: none;
    background-color: #fff;
    border: none;
    color: hsl(var(--grey-800));
    cursor: pointer;
    -webkit-appearance: none;
  }
`;
