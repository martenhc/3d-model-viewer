import {css} from 'lit';

export const styles = css`
  :host {
    display: block;
    font-size: 1.4rem;
    margin: 0 auto;
    padding: 2rem;
  }

  .controls-wrapper {
    color: var(--blue-600);
    display: block;
    font-size: 1.4rem;
    padding-top: 2rem;
    position: absolute;
    width: 24.8rem;
  }

  .add-text {
    margin-left: 0.8rem;
  }

  .script-display-wrapper {
    bottom: 0;
    position: fixed;
  }
`;
