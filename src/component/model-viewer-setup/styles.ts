import {css} from 'lit';

export const styles = css`
  :host {
    display: block;
    font-size: 1.4rem;
    margin: 0 auto;
    padding: 2rem;
  }

  .controls-wrapper {
    padding-top: 2rem;
    display: block;
    position: absolute;
    color: var(--blue-600);
    font-size: 1.4rem;
  }

  .add-text {
    margin-left: 0.8rem;
  }
`;
