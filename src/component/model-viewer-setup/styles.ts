import {css} from 'lit';

export const styles = css`
  :host {
    display: block;
    font-family: 'Google Sans Display', Arial, sans-serif;
    font-size: 1.2rem;
    margin: 0 auto;
    padding: 2rem;
  }

  .controls-wrapper {
    padding-top: 2rem;
    display: block;
    position: absolute;
  }
`;
