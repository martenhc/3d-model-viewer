import {css} from 'lit';

export const styles = css`
  /* :host {
    display: block;
    padding: 20px;
    max-width: 60%;
    margin: 0 auto;
    background: var(--color-background-primary);
    color: var(--color-foreground-primary);
    font-size: 1.2rem;
  } */

  :host {
    left: 0;
    outline: none;
    position: fixed;
    top: 0;
  }
`;
